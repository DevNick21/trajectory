name: uk_cv_skill
description: |
  Tailor a UK CV to a specific role + company, ground every bullet in
  the user's career history, and produce .docx + .pdf + LaTeX-PDF
  deliverables.

# Progressive-disclosure: the high-level instructions load on every
# invocation; per-script details only load when the script is invoked.
instructions: |
  You are the UK CV writer. Use the user's career entries (FAISS-
  retrieved + STAR-boosted) to draft bullets that match the JD's
  required skills. Cite every bullet to a specific career_entry_id.
  Run self_audit on the draft. Render via the three renderer scripts
  in this skill — produce all three formats.

scripts:
  - id: cv_tailor
    path: ../sub_agents/cv_tailor.py
    entry: generate
    description: Generate the structured CVOutput from JD + entries.
  - id: cv_tailor_advisor
    path: ../managed/cv_tailor_advisor.py
    entry: run
    description: Generate via the Advisor-tool-paired session (preferred when available).
  - id: render_cv_docx
    path: ../renderers/cv_docx.py
    entry: render_cv_docx
    description: Render CVOutput to a .docx file.
  - id: render_cv_pdf
    path: ../renderers/cv_pdf.py
    entry: render_cv_pdf
    description: Render CVOutput to a .pdf file via reportlab.
  - id: render_latex_pdf
    path: ../renderers/cv_latex.py
    entry: render_latex_pdf
    description: Render CVOutput to a typeset PDF via LaTeX.
  - id: self_audit
    path: ../sub_agents/self_audit.py
    entry: run
    description: Audit the generated CV for clichés, unsupported claims, style fit.
