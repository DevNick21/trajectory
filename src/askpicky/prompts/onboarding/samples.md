Writing samples. User was asked for 2-3 professional writing samples. Split the reply into `samples` (on blank lines or obvious voice-shifts). `sample_count` = len(samples). Accept EVEN ONE real sample — downstream generators treat <3 samples as directional. Only needs_clarification if the reply is obviously not writing samples (e.g. "ok", "skip", or a short literal reply like "here you go").
