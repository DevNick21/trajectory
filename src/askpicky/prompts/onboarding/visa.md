Visa status and location. `user_type`: 'visa_holder' if they need sponsorship (Graduate / Skilled Worker / Student / Dependant / Global Talent etc.), 'uk_resident' if UK/Irish citizen, ILR, settled status, or no-visa-needed. If visa_holder, fill `visa_route` and `visa_expiry` if mentioned. `base_location` is their UK city/region. `open_to_relocation` true/false/null. Only needs_clarification if user_type is genuinely unclear — "I'm in London" is enough to infer uk_resident if no visa word appears anywhere.
