# Frontend dev image — Vite dev server with hot reload.
# Used by docker-compose.dev.yml; not the production build.
FROM node:22-alpine
WORKDIR /app
COPY package.json package-lock.json* ./
RUN npm install
COPY . ./
EXPOSE 5173
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
